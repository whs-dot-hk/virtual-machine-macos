#cloud-config
hostname: debian
users:
  - name: packer
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users, admin
    shell: /bin/bash
    lock_passwd: false
    plain_text_passwd: packer
ssh_pwauth: true
chpasswd:
  expire: false
